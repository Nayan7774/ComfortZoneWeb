package com.example.demo;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class TestDB {
	 public static void main(String[] args) {
			/*
			 * // JDBC URL, username, and password String url =
			 * "jdbc:mysql://localhost:3306/interior_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC";
			 * String username = "root"; // replace with your DB username String password =
			 * "MYSQL"; // replace with your DB password
			 * 
			 * try { // Try to connect Connection conn = DriverManager.getConnection(url,
			 * username, password);
			 * System.out.println("✅ Connected to MySQL successfully!"); conn.close(); }
			 * catch (SQLException e) { System.err.println("❌ Connection failed:");
			 * e.printStackTrace(); }
			 */
	    }
}
